const mirnaRoute = (req,res) => {
  res.send("Mirna Parra Lozano");
};

const aronRoute = (req,res) => {
  res.send("Aaron Parra Lozano");
};

const moiRoute = (req,res) => {
  res.send("Moises Parra Lozano");
};

module.exports = {
  mirnaRoute,
  aronRoute,
  moiRoute,

};